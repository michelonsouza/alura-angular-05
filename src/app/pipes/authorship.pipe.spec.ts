import { AuthorshipPipe } from './authorship.pipe';

describe('AuthorshipPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorshipPipe();
    expect(pipe).toBeTruthy();
  });
});
